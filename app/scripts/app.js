'use strict';

angular.module('ecApp', [
  'ngResource',
  'btford.socket-io',
  'angularFileUpload'
]);
  /*.config(function ($routeProvider) {
    $routeProvider
      .when('/', {
        templateUrl: 'views/main.html',
        controller: 'MainCtrl'
      })
      .otherwise({
        redirectTo: '/'
      });
  }); */
